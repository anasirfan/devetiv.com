/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['static.ghost.org', "images.unsplash.com"], // Add the domain here
      },
};

export default nextConfig;
